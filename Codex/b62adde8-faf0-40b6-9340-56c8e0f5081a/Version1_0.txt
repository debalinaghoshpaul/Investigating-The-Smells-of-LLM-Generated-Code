```java
import java.util.*;

public class WordCount {

    public static void main(String[] args) {
        // Create a map to store the word counts
        Map<String, List<Integer>> wordCounts = new HashMap<>();

        // Read the input
        Scanner scanner = new Scanner(System.in);
        while (scanner.hasNextLine()) {
            String line = scanner.nextLine();
            String[] parts = line.split(" ");
            String word = parts[0];
            int pageNumber = Integer.parseInt(parts[1]);

            // Add the word to the map
            List<Integer> pageNumbers = wordCounts.getOrDefault(word, new ArrayList<>());
            pageNumbers.add(pageNumber);
            wordCounts.put(word, pageNumbers);
        }

        // Print the word counts
        for (Map.Entry<String, List<Integer>> entry : wordCounts.entrySet()) {
            System.out.println(entry.getKey() + ": " + entry.getValue());
        }
    }
}
```

**Test cases:**

* **Normal case:**
```
the 1
the 2
programming 3
language 3
you 4
need 4
to 5
use 5
is 6
java 6
```
**Output:**

```
java: [6]
is: [6]
language: [3]
need: [4]
programming: [3]
the: [1, 2]
to: [5]
use: [5]
you: [4]
```

* **Empty input:**
```
```
**Output:**

```
```

* **Corner case: word with multiple spaces:**
```
word with multiple spaces 7
```
**Output:**

```
word with multiple spaces: [7]
```
