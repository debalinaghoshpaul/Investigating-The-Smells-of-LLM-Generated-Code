```java
import java.util.Scanner;

public class MultiplicationTable {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Get the number to generate the multiplication table for
        System.out.println("Input the number (Table to be calculated) : ");
        int number = scanner.nextInt();

        // Get the number of terms to generate in the multiplication table
        System.out.println("Input number of terms : ");
        int terms = scanner.nextInt();

        // Generate and display the multiplication table
        for (int i = 0; i <= terms; i++) {
            System.out.println(number + " X " + i + " = " + (number * i));
        }
    }
}
```

## Test Cases

**Normal Case:**

Input:
```
Input the number (Table to be calculated) : 5
Input number of terms : 5
```

Output:
```
5 X 0 = 0
5 X 1 = 5
5 X 2 = 10
5 X 3 = 15
5 X 4 = 20
5 X 5 = 25
```

**Corner Cases:**

* **Input number is 0:**

Input:
```
Input the number (Table to be calculated) : 0
Input number of terms : 5
```

Output:
```
0 X 0 = 0
0 X 1 = 0
0 X 2 = 0
0 X 3 = 0
0 X 4 = 0
0 X 5 = 0
```

* **Input number is negative:**

Input:
```
Input the number (Table to be calculated) : -5
Input number of terms : 5
```

Output:
```
-5 X 0 = 0
-5 X 1 = -5
-5 X 2 = -10
-5 X 3 = -15
-5 X 4 = -20
-5 X 5 = -25
```

* **Input number of terms is 0:**

Input:
```
Input the number (Table to be calculated) : 5
Input number of terms : 0
```

Output:
```
5 X 0 = 0
```
