

You can copy and paste this code into a Java file and run it to test the functionality of the `AnimalSpecies` class. The test program demonstrates creating instances of `AnimalSpecies`, accessing and updating their attributes, and using the `endangered` and `equals` methods.