

This program creates an array of integers and then checks if the array contains both specified elements, 65 and 77. It iterates through the array and sets flags if it finds the specified elements. Finally, it prints whether the array contains both elements or not.