```java
import java.util.Scanner;

public class CheckCharacterBeforeAfter {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Get the input string
        System.out.println("Enter a string: ");
        String str = scanner.nextLine();

        // Get the character to check
        System.out.println("Enter the character to check: ");
        char ch = scanner.next().charAt(0);

        // Check if the character is present in the string
        int index = str.indexOf(ch);
        if (index == -1) {
            System.out.println("The character is not present in the string.");
        } else {
            // Check if the character immediately before and after the specified character is the same
            boolean isSame = false;
            if (index > 0 && index < str.length() - 1) {
                if (str.charAt(index - 1) == str.charAt(index + 1)) {
                    isSame = true;
                }
            }

            // Print the result
            if (isSame) {
                System.out.println("The character immediately before and after the specified character is the same.");
            } else {
                System.out.println("The character immediately before and after the specified character is not the same.");
            }
        }
    }
}
```

**Test Cases:**

**Valid Test Cases:**

* Input: "abcabc" and ch = 'b'
  * Output: The character immediately before and after the specified character is the same.
* Input: "123456" and ch = '2'
  * Output: The character immediately before and after the specified character is the same.
* Input: "hello" and ch = 'l'
  * Output: The character immediately before and after the specified character is the same.

**Corner Cases:**

* Input: "" and ch = 'a'
  * Output: The character is not present in the string.
* Input: "a" and ch = 'b'
  * Output: The character immediately before and after the specified character is not the same.
* Input: "abcabc" and ch = 'c'
  * Output: The character immediately before and after the specified character is the same.
* Input: "123456" and ch = '6'
  * Output: The character immediately before and after the specified character is the same.
* Input: "hello" and ch = 'h'
  * Output: The character immediately before and after the specified character is not the same.
