

In this program, we create a TreeMap called `treeMap` and add some key-value pairs to it. We then specify a key to search using the `keyToSearch` variable. We use the `containsKey(key)` method to check if the key exists in the TreeMap. If it exists, we retrieve the corresponding value using the `get(key)` method. Finally, we print the result whether the key was found or not.