

This program creates a stack, pushes some elements onto the stack, and then moves the nth element from the top of the stack to the top using the `moveNthElementToTop` method. The output will show the original stack and the stack after moving the nth element to the top.