

This program creates a stack of integers, pushes some elements onto the stack, and then checks if a given element is present in the stack using the `isElementPresent` method. The `isElementPresent` method utilizes the `contains` method provided by the Stack class to check for the presence of the element. Finally, the program prints whether the element is present in the stack or not.