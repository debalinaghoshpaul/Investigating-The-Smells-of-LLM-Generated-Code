

You can create a text file named `ratings.txt` with sample ratings and run this program to calculate the average rating for each product. The program reads in the file, extracts each rating using the `StringTokenizer` class, and outputs the average rating for each product. Customers that did not rate a product are not considered when computing the average rating.