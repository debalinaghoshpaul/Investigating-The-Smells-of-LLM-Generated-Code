

This program creates a TreeMap, adds some key-value pairs to it, and then calls the `getLeastKey()` method to find the least key greater than or equal to the target value. The method uses the `ceilingKey()` method of the TreeMap to get the desired key. It returns null if there is no such key. In the example above, the output will be:

