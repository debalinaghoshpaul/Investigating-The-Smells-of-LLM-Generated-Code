

This program defines a method `findCommonElements` that takes three sorted arrays as input and finds the common elements in them by iterating through the arrays using three pointers. Finally, the program prints out the common elements found.