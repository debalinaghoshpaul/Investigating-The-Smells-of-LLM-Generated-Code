**Java program:**

```java
import java.util.Scanner;

public class CompareStringsIgnoringCase {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Get the two strings from the user
        System.out.println("Enter the first string:");
        String firstString = scanner.nextLine();

        System.out.println("Enter the second string:");
        String secondString = scanner.nextLine();

        // Compare the two strings, ignoring case
        boolean areEqual = firstString.equalsIgnoreCase(secondString);

        // Print the result
        if (areEqual) {
            System.out.println("The two strings are equal, ignoring case.");
        } else {
            System.out.println("The two strings are not equal, ignoring case.");
        }
    }
}
```

**Test cases including corner cases:**

* **Test case 1:**
    * First string: "Hello"
    * Second string: "hello"
    * Expected output: "The two strings are equal, ignoring case."
* **Test case 2:**
    * First string: "123"
    * Second string: "12345"
    * Expected output: "The two strings are not equal, ignoring case."
* **Test case 3:**
    * First string: " " (empty string)
    * Second string: " " (empty string)
    * Expected output: "The two strings are equal, ignoring case."
* **Test case 4:**
    * First string: null
    * Second string: "hello"
    * Expected output: "The two strings are not equal, ignoring case."
* **Test case 5:**
    * First string: "hello"
    * Second string: null
    * Expected output: "The two strings are not equal, ignoring case."
* **Test case 6:**
    * First string: "Hello"
    * Second string: "HELLO"
    * Expected output: "The two strings are equal, ignoring case."
