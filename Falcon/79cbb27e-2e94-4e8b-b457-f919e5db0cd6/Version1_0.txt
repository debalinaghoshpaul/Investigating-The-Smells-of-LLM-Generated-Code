```java
import java.util.*;

public class HappyNumber {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.println("Enter a number: ");
        int number = scanner.nextInt();

        if (isHappy(number)) {
            System.out.println(number + " is a happy number.");
        } else {
            System.out.println(number + " is not a happy number.");
        }
    }

    public static boolean isHappy(int number) {
        Set<Integer> seenNumbers = new HashSet<>();

        while (number != 1) {
            int sumOfSquares = 0;
            while (number > 0) {
                int digit = number % 10;
                sumOfSquares += digit * digit;
                number /= 10;
            }

            if (seenNumbers.contains(sumOfSquares)) {
                return false;
            }

            seenNumbers.add(sumOfSquares);
            number = sumOfSquares;
        }

        return true;
    }
}
```

Test Cases:

### Positive Test Cases:

| Input | Expected Output |
|---|---|
| 1 | "1 is a happy number." |
| 7 | "7 is a happy number." |
| 10 | "10 is a happy number." |
| 28 | "28 is a happy number." |
| 496 | "496 is a happy number." |

### Negative Test Cases:

| Input | Expected Output |
|---|---|
| 2 | "2 is not a happy number." |
| 3 | "3 is not a happy number." |
| 4 | "4 is not a happy number." |
| 5 | "5 is not a happy number." |
| 11 | "11 is not a happy number." |

### Corner Cases:

| Input | Expected Output |
|---|---|
| 0 | "0 is not a happy number." |
| -1 | "Invalid input. The number must be a positive integer." |
